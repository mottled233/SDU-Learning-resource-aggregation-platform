<% provide(:title, "#{@course.course_name}—资源管理") %>
<div class="container-fluid-full"> 

    <div class="row-fluid">
      <%= render :partial => "users/aside" %>
      <div id="content" class= "row-fluid shadow-box min-height-control" data-min-height='680'>
        <p id="notice"><%= notice %></p>
        
        <h1><%=@course.course_name%>—全部资源</h1>
        <hr>
        
        <%= render :partial => "resources/resource_list", :locals => { :resources => @resource } %>
        
        <br>
        

        <hr>
        <%= link_to '返回',  teachers_teachers_space_path(@teacher.id) %>
      </div>
    </div>
    
    
</div> 

<div class="clearfix"></div>