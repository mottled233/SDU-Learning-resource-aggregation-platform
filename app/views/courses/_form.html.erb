<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_name %>
    <%= f.text_field :course_name %>
  </div>

  <div class="field">
    <%= f.label :knowledge %>
    <%= f.text_field :knowledge %>
  </div>

  <div class="field">
    <%= f.label :teacher %>
    <%= f.text_field :teacher %>
  </div>

  <div class="field">
    <%= f.label :teacher_relationship %>
    <%= f.text_field :teacher_relationship %>
  </div>

  <div class="field">
    <%= f.label :department_id %>
    <%= f.text_field :department_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
